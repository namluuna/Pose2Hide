using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OnFloorSpawner : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //ObjectPool.instance.SpawnFromPool("OnFloor", transform.position, Quaternion.identity);
        //ObjectPool.instance.SpawnFromPool("OnFloor2", transform.position, Quaternion.identity);
    }
}
